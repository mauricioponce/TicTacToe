<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <corners
        android:bottomLeftRadius="@dimen/grid_corners"
        android:bottomRightRadius="@dimen/grid_corners"
        android:topLeftRadius="@dimen/grid_corners"
        android:topRightRadius="@dimen/grid_corners" />

    <stroke
        android:width="0.5dp"
        android:color="@color/colorPrimary" />

    <solid android:color="@android:color/white" />

</shape>
